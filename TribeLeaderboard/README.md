# TribeLeaderboard

Awards random configurable points when one player-owned tribe destroys an enemy structure.
Own/environmental destruction is ignored and repeated awards against the same victim tribe are rate-limited.

Chat: `/top`, `/rank`.
Console: `TribeLeaderboard.Reload`, `TribeLeaderboard.Add <team> <points>`, `TribeLeaderboard.Reset`.
