# ResourceLogistics

`/pull <resource> <quantity>` safely pulls ordinary resources from nearby same-tribe storage.
`/distribute` moves carried resources into nearby matching Dedicated Storage.

The plugin respects player weight and inventory limits, never moves blueprints/equipment, and rolls back partial transfers.
