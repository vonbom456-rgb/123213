# DinoBalance

Enforces a hard Magmasaur (`Cherufe_Character_BP`) maximum of 328,000 health and
6,000 final outgoing damage per hit. The health cap is applied on spawn/uncryo
and periodically, so later level-ups cannot bypass it.

Server console command: `DinoBalance.Reload`.
