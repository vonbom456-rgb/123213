# GathererRouter

Look at a tribe container and use `/router hub`, `/router source`, or `/router target <resource>`.
`/router auto` toggles automatic Resource Gatherers discovery. `/router status` and `/router clear` manage the route.
Items move source -> hub -> assigned/matching S+ Dedicated Storage with measured transfer and rollback.
Console: `GathererRouter.Reload`.
